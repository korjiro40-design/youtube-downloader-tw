name: Build Windows Portable

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'main.py'
      - 'requirements.txt'
      - '.github/workflows/build-windows.yml'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Download FFmpeg
        shell: powershell
        run: |
          Invoke-WebRequest -Uri "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip" -OutFile ffmpeg.zip
          Expand-Archive ffmpeg.zip -DestinationPath ffmpeg-temp
          $ff = Get-ChildItem -Path ffmpeg-temp -Recurse -Filter ffmpeg.exe | Select-Object -First 1
          Copy-Item $ff.FullName .\ffmpeg.exe

      - name: Build EXE
        shell: powershell
        run: |
          pyinstaller --noconfirm --clean --onefile --windowed --name "YouTubeDownloaderTW" --add-binary "ffmpeg.exe;." --collect-all yt_dlp main.py

      - name: Prepare portable package
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force portable
          Copy-Item "dist\YouTubeDownloaderTW.exe" "portable\YouTubeDownloaderTW.exe"
          Copy-Item "README.md" "portable\README.md"

      - name: Upload portable package
        uses: actions/upload-artifact@v4
        with:
          name: YouTubeDownloaderTW-Windows-Portable
          path: portable/
          if-no-files-found: error
